class ca{
    constructor (name, weight, maxJumpHeigth, verticalPosition){
        this.name = nam 
        this.weight= weight
        this.maxJumpHeigth = maxJumpHeigth
        this.verticalPosition = verticalPosition
    }
    //params power number // entre 0 e 1
    jump (power)
    const jumpHeight = this.maxJumpHeigth * power
    this.verticalPosition += jumpHeight
    
}